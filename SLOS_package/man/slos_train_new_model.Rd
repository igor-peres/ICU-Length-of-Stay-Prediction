% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05-Train.R
\name{slos_train_new_model}
\alias{slos_train_new_model}
\title{Train and stack caret models for ICU LOS (LM + Ranger -> Ranger stack)}
\usage{
slos_train_new_model(
  train,
  outcome = "UnitLengthStay_trunc",
  test = NULL,
  metric = "RMSE",
  cv_folds = 5,
  cv_method = "cv",
  verbose_iter = TRUE,
  seed = 998,
  base_ranger_grid = NULL,
  meta_method = "ranger",
  meta_grid = NULL,
  save_path = NULL,
  drop_single_level = TRUE,
  encode_categoricals = TRUE,
  continue_on_fail = TRUE
)
}
\arguments{
\item{train}{A data.frame with predictors and the outcome column.}

\item{outcome}{Character scalar. Name of the outcome column used for training.
Default `"UnitLengthStay_trunc"`.}

\item{test}{Optional data.frame to evaluate on. Must contain the same
predictors as `train` (at least the columns used by the final model) and
the `outcome` column for metric computation.}

\item{metric}{Metric optimized during training (passed to caret). Default `"RMSE"`.}

\item{cv_folds}{Number of CV folds. Default `5`.}

\item{cv_method}{Resampling method for `trainControl()`. Default `"cv"`.}

\item{verbose_iter}{Logical; print training progress. Default `TRUE`.}

\item{seed}{Integer seed for reproducibility. Default `998`.}

\item{base_ranger_grid}{Optional `data.frame` for the base `ranger` model's
grid (columns `.mtry`, `.splitrule`, `.min.node.size`). If `NULL`, uses:
`.mtry = 5:10`, `.splitrule = "variance"`, `.min.node.size = 5`.}

\item{meta_method}{Stacking meta-learner method. Default `"ranger"`.}

\item{meta_grid}{Optional `data.frame` of hyperparameters for the meta-learner.
If `NULL`, uses:
`expand.grid(mtry = 2, min.node.size = c(5,10,15,20), splitrule = c("variance","extratrees","maxstat"))`.}

\item{save_path}{Optional file path to save the stacked model (`.RData` will
be appended if missing). If `NULL`, the model is not saved. Default `NULL`.}

\item{drop_single_level}{Logical; drop factor predictors with <2 levels
(computed on the full training data) before encoding. Default TRUE.}

\item{encode_categoricals}{Logical; if TRUE, use dummyVars(fullRank=TRUE) to
one-hot encode non-numeric predictors. Default TRUE.}

\item{continue_on_fail}{Logical; pass through to caretEnsemble::caretList()
  so that failed base models don't abort the run. Default TRUE.
(All other parameters are unchanged.)}
}
\value{
A list with:
\describe{
  \item{stacked_model}{The fitted `caretStack` object.}
  \item{base_models}{The fitted `caretList` of base learners.}
  \item{train_control}{The `trainControl` used.}
  \item{metrics}{If `test` provided: data.frame with RMSE, MAE, R2.}
  \item{predictions}{If `test` provided: numeric vector of predictions.}
}
}
\description{
Fits a base ensemble (linear model and random forest) with `caretEnsemble::caretStack()` (default: `ranger`). Optionally evaluates on
a testing set and saves the final stacked model to disk.
}
\examples{
\donttest{
data(SampledData)
idx <- caret::createDataPartition(sampled_data$UnitLengthStay_trunc, p=.8, list=FALSE)
training  <- sampled_data[idx, ]
testing  <- sampled_data[-idx, ]

fit <- slos_train_new_model(training, outcome = "UnitLengthStay_trunc", test = testing,
                        save_path = "slos_trained_stack.RData")
fit$metrics

unlink("slos_trained_stack.RData")

}

}
