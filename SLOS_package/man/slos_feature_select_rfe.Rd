% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04-FeatureSelection.R
\name{slos_feature_select_rfe}
\alias{slos_feature_select_rfe}
\title{Recursive Feature Elimination (RFE) for ICU Length of Stay (LOS) Prediction}
\usage{
slos_feature_select_rfe(
  data = NULL,
  outcome = NULL,
  x = NULL,
  y = NULL,
  method = c("ranger", "treebag", "rf"),
  sizes = NULL,
  normalize = FALSE,
  preproc_methods = c("range"),
  cv_folds = 5,
  metric = "RMSE",
  seed = NULL,
  verbose = TRUE,
  tune_length = 5,
  num_trees = 500,
  importance = "permutation",
  respect_unordered_factors = "order",
  min_node_size = NULL,
  splitrule = NULL,
  ...
)
}
\arguments{
\item{data}{Optional `data.frame`/`tibble` containing predictors **and** the outcome.}

\item{outcome}{Optional single string naming the outcome column in `data`.}

\item{x}{Optional predictor matrix/data.frame. If provided, `y` must be provided too.}

\item{y}{Optional numeric vector outcome. Required when `x` is provided.}

\item{method}{Character scalar: `"treebag"` (default) or `"ranger"`.}

\item{sizes}{Integer vector of subset sizes to evaluate during RFE.
Defaults to `c(10:30, 35, 40, 45, 50, 60, 80)` for `"treebag"` and
`c(10:30, 40, 60)` for `"rf"`, matching the original script.}

\item{normalize}{Logical; if `TRUE`, fit a `preProcess` object on predictors
and use it inside RFE. Default `FALSE` for trees.}

\item{preproc_methods}{Character vector passed to `caret::preProcess`.
Default `c("range")`.}

\item{cv_folds}{Integer; number of CV folds for RFE (`rfeControl(method="cv")`).
Default `5`.}

\item{metric}{Optimization metric for RFE, usually `"RMSE"` for regression.
Default `"RMSE"`.}

\item{seed}{Integer for reproducibility (passed to `set.seed()`).
Default `420` for `"treebag"` and `100` for `"rf"`, mirroring the script.}

\item{verbose}{Logical; print progress. Default `TRUE`.}

\item{tune_length}{Integer; passed to `caret::train()` when `method = "ranger"`
to control the number of `mtry` values tried. Default `5`.}

\item{num_trees}{Integer; number of trees for `ranger` (passed as `num.trees`).
Default `500`.}

\item{importance}{Character; variable-importance type for `ranger`:
`"none"`, `"impurity"`, or `"permutation"` (default).}

\item{respect_unordered_factors}{Character; how `ranger` handles unordered factors:
e.g., `"order"` (default). See `ranger::ranger()`.}

\item{min_node_size}{Optional integer; `ranger` minimal node size
(passed as `min.node.size`). If `NULL`, caret's default is used.}

\item{splitrule}{Optional character; `ranger` split rule (e.g., `"variance"` for regression).
If `NULL`, caret's default is used.}

\item{...}{Additional arguments forwarded to `caret::train()` (only relevant when
`method = "ranger"`, e.g., `tuneGrid`, `maximize`, `metric`, etc.).}
}
\value{
An object of class `slos_rfe_result`, a list with:
\describe{
  \item{selected_vars}{Character vector of selected predictor names.}
  \item{rfe}{The `caret::rfe` result object.}
  \item{preproc}{The `caret::preProcess` object if normalization was used; else `NULL`.}
  \item{meta}{List of metadata (method, sizes, cv_folds, metric, normalize, timestamp).}
}
}
\description{
A wrapper around `caret::rfe()` to perform feature selection for
ICU Length of Stay modeling. Supports Treebag (`treebag`) and Random Forest
(`ranger`), optional normalization (via `caret::preProcess`), and k-fold cross-validation. 
Returns selected variables and artifacts so you can consistently transform new datasets.
}
\examples{
\donttest{
data(SampledData)

res <- slos_feature_select_rfe(
  data    = sampled_data,
  outcome = "UnitLengthStay_trunc",
  method  = "treebag",
  normalize = TRUE
)

res$selected_vars
}
}
