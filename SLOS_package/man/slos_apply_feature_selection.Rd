% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04-FeatureSelection.R
\name{slos_apply_feature_selection}
\alias{slos_apply_feature_selection}
\title{Apply SLOS Feature Selection to New Data}
\usage{
slos_apply_feature_selection(newdata, fs_result, strict = TRUE)
}
\arguments{
\item{newdata}{A `data.frame`/`tibble` with at least the selected columns.}

\item{fs_result}{An object returned by [slos_feature_select_rfe()].}

\item{strict}{Logical; if `TRUE`, error when selected columns are missing in
`newdata`. If `FALSE`, silently drop missing and warn. Default `TRUE`.}
}
\value{
A data.frame with (optionally normalized) predictors restricted to
  the selected variables in the same order the RFE produced.
}
\description{
Given a fitted `slos_rfe_result`, apply the same pre-processing (if any) and
reduce `newdata` to the selected predictors in the correct order.
}
\examples{
\dontrun{
data(SampledData)
x_new <- slos_apply_feature_selection(sampled_data, res)
}
}
